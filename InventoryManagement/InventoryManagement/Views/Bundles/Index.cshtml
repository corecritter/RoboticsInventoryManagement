@model IEnumerable<InventoryManagement.Database.Bundles>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.BundleName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.BundleName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BundleId }) |
            @Html.ActionLink("Details", "Details", new { id=item.BundleId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BundleId })
        </td>
    </tr>
}

</table>
